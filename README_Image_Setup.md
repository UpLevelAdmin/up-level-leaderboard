# 📸 คู่มือการตั้งค่ารูปภาพสำหรับระบบการ์ดฝากขาย

## 🎯 วิธีที่ดีที่สุด: ใช้ Google Drive

### ขั้นตอนการทำ

#### 1. อัพโหลดรูปภาพไปที่ Google Drive

1. เปิด Google Drive: https://drive.google.com
2. สร้างโฟลเดอร์ใหม่ชื่อ "การ์ดฝากขาย" (หรือชื่ออื่นที่ต้องการ)
3. อัพโหลดรูปภาพการ์ดทั้งหมดเข้าไปในโฟลเดอร์นี้

#### 2. ตั้งค่าแชร์รูปภาพเป็น Public

**วิธีที่ 1: ตั้งค่าทีละไฟล์**
1. คลิกขวาที่รูปภาพ → **Share** (แชร์)
2. เปลี่ยนจาก "Restricted" เป็น **"Anyone with the link"**
3. เลือก **"Viewer"** (ผู้ดู)
4. คลิก **"Copy link"** (คัดลอกลิงก์)
5. คลิก **"Done"** (เสร็จสิ้น)

**วิธีที่ 2: ตั้งค่าทั้งโฟลเดอร์ (แนะนำ)**
1. คลิกขวาที่โฟลเดอร์ → **Share**
2. เปลี่ยนเป็น **"Anyone with the link"** → **"Viewer"**
3. คลิก **"Done"**
4. ทุกไฟล์ในโฟลเดอร์จะสามารถเข้าถึงได้ทันที

#### 3. แปลง URL ให้เป็น Direct Link

URL ที่ได้จาก Google Drive จะเป็นแบบนี้:
```
https://drive.google.com/file/d/FILE_ID/view?usp=sharing
```

ต้องแปลงเป็น Direct Link:
```
https://drive.google.com/uc?export=view&id=FILE_ID
```

**วิธีแปลง:**
- คัดลอก `FILE_ID` จาก URL เดิม (ส่วนที่อยู่ระหว่าง `/d/` และ `/view`)
- ใส่ในรูปแบบ: `https://drive.google.com/uc?export=view&id=FILE_ID`

**ตัวอย่าง:**
- URL เดิม: `https://drive.google.com/file/d/1ABC123xyz456/view?usp=sharing`
- Direct Link: `https://drive.google.com/uc?export=view&id=1ABC123xyz456`

#### 4. ใช้ใน Google Sheets

**วิธีที่ 1: ใช้สูตร =IMAGE() (แนะนำ)**
- ใส่สูตรใน cell: `=IMAGE("https://drive.google.com/uc?export=view&id=FILE_ID")`
- รูปภาพจะแสดงใน cell และ Apps Script จะดึง URL ได้อัตโนมัติ

**วิธีที่ 2: ใส่ URL โดยตรง**
- วาง Direct Link URL ใน cell โดยตรง
- Apps Script จะดึง URL ไปใช้ได้

---

## 📋 ตัวอย่างการใช้งาน

### ใน Google Sheets:

| คอลัมน์ | ตัวอย่าง |
|---------|---------|
| ชื่อการ์ด | พิคาชู UR |
| ภาพการ์ด | `=IMAGE("https://drive.google.com/uc?export=view&id=1ABC123xyz456")` |
| หรือ | `https://drive.google.com/uc?export=view&id=1ABC123xyz456` |

---

## ⚠️ ข้อควรระวัง

1. **ต้องตั้งค่าเป็น Public**: ถ้าไม่ตั้งค่าเป็น "Anyone with the link" รูปภาพจะไม่แสดงในเว็บ
2. **ใช้ Direct Link**: ต้องใช้ Direct Link (`/uc?export=view&id=`) ไม่ใช่ sharing link (`/file/d/.../view`)
3. **ขนาดไฟล์**: แนะนำให้ใช้รูปภาพขนาดไม่เกิน 5MB เพื่อความเร็วในการโหลด
4. **รูปแบบไฟล์**: รองรับ JPG, PNG, GIF, WebP

---

## 🔧 วิธีแปลง URL อัตโนมัติ

ถ้ามี URL หลายตัว สามารถใช้สูตรใน Google Sheets:

```
=REGEXREPLACE(A1, "https://drive\.google\.com/file/d/([^/]+)/.*", "https://drive.google.com/uc?export=view&id=$1")
```

โดยที่ A1 คือ cell ที่มี URL เดิม

---

## ✅ ตรวจสอบว่า URL ถูกต้อง

1. เปิด URL ในเบราว์เซอร์ใหม่ (Incognito/Private mode)
2. ถ้าเห็นรูปภาพแสดง = ถูกต้อง ✅
3. ถ้าเห็นหน้า "You need access" = ยังไม่ได้ตั้งค่าเป็น Public ❌

---

## 🚀 Tips เพิ่มเติม

1. **ตั้งชื่อไฟล์ให้เป็นระเบียบ**: เช่น `Pikachu_UR_001.jpg` เพื่อให้หาได้ง่าย
2. **ใช้โฟลเดอร์แยกตามประเภท**: เช่น "SAR", "UR", "SR" เพื่อความเป็นระเบียบ
3. **บีบอัดรูปภาพ**: ใช้ tools เช่น TinyPNG เพื่อลดขนาดไฟล์แต่ยังคงคุณภาพ
4. **Backup**: สำรองรูปภาพไว้ในที่อื่นด้วย เผื่อ Drive มีปัญหา

---

## 📞 ถ้ายังมีปัญหา

1. ตรวจสอบว่า URL เป็น Direct Link หรือไม่
2. ตรวจสอบว่าไฟล์ตั้งค่าเป็น "Anyone with the link" แล้วหรือยัง
3. ลองเปิด URL ใน Incognito mode เพื่อทดสอบ
4. ตรวจสอบ console ในเบราว์เซอร์ (F12) เพื่อดู error messages

