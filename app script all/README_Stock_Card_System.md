# 📋 คู่มือระบบจัดการสต็อกการ์ดโปเกมอน

## 📊 สรุปขั้นตอนการทำงาน

### 🔄 Workflow หลัก: ระบบ Hard Copy + Batch Import

#### **ขั้นตอนที่ 1: เริ่มวันทำงาน**
1. **พิมพ์ฟอร์ม Hard Copy**
   - เมนู → **🖨️ พิมพ์ฟอร์มรับการ์ดเข้า** (สำหรับการ์ดที่เข้ามา)
   - เมนู → **🖨️ พิมพ์ฟอร์มส่งการ์ดออก** (สำหรับการ์ดที่ออก)
   - ระบบจะสร้างชีทใหม่พร้อมฟอร์มแก้ไขได้
   - แก้ไข/ปรับแต่งได้ก่อนพิมพ์
   - พิมพ์: File → Print หรือ Ctrl+P

#### **ขั้นตอนที่ 2: หน้างาน (Hard Copy)**
- พนักงานกรอกข้อมูลในฟอร์มที่พิมพ์ออกมา
- เก็บฟอร์มไว้เป็นเอกสารอ้างอิง

#### **ขั้นตอนที่ 3: จบวัน (กรอกข้อมูลในระบบ)**
1. **เปิดชีท DailyInput**
   - เมนู → **📋 เปิดชีท DailyInput**
   - ระบบจะสร้างชีทถ้ายังไม่มี
   - กรอกข้อมูลจากฟอร์ม Hard Copy ลงในชีท DailyInput
   - **คอลัมน์สำคัญ:**
     - วันที่, เวลา, ผู้ทำรายการ
     - ประเภท (เข้า/ออก) - มี Data Validation
     - ชุดการ์ด, รหัสชุด, เลขการ์ด, ชื่อการ์ด
     - จำนวน, ราคา, เหตุผล, หมายเหตุ
     - สถานะ (รอตรวจ/นำเข้าแล้ว/ยกเลิก) - ตั้งค่าเริ่มต้นเป็น "รอตรวจ"

#### **ขั้นตอนที่ 4: Batch Import (นำเข้าสต็อก)**
1. **ตรวจสอบข้อมูล**
   - เมนู → **📥 นำเข้าข้อมูลรายวัน (Batch Import)**
   - กด **🔍 ตรวจสอบข้อมูลก่อนนำเข้า**
   - ระบบจะแสดงสรุป:
     - จำนวนรายการที่พร้อมนำเข้า
     - จำนวนการ์ดเข้า/ออก
     - ข้อผิดพลาด (ถ้ามี)

2. **นำเข้าข้อมูล**
   - กด **📥 เริ่มนำเข้าข้อมูล**
   - ระบบจะ:
     - อัพเดท Master sheet อัตโนมัติ
     - เพิ่มการ์ดใหม่ (ถ้าไม่มี)
     - อัพเดทจำนวน (เพิ่มหรือลดตามประเภท)
     - บันทึก Log ครบถ้วน
     - อัพเดทสถานะเป็น "นำเข้าแล้ว"

---

## 🎯 ฟีเจอร์หลัก

### 1. **การจัดการการ์ด (Individual Cards)**
- **🎴 เพิ่มการ์ดแบบง่าย**: เพิ่มการ์ดทีละใบ พร้อม auto-fill
- **✏️ อัพเดทข้อมูลสินค้า**: แก้ไขราคา, ที่เก็บ, สภาพ, หมายเหตุ
- **📤 เอาของออก**: ลดจำนวนและบันทึกเหตุผล

### 2. **การนำเข้าข้อมูล (Bulk Import)**
- **📥 นำเข้าข้อมูลจากชีทชุดการ์ด**: นำเข้าจากชีทชุดการ์ด (เช่น MA1T, MA2T)
- **📥 นำเข้าข้อมูลรายวัน**: Batch import จาก DailyInput

### 3. **การจัดการ Sets**
- **🔄 สร้าง/อัปเดต Sets จาก Master**: สร้าง/อัปเดตชีท Sets อัตโนมัติจาก Master

### 4. **การจัดการชีท**
- **🧹 สรุปชีทและคำแนะนำ**: ดูสรุปชีททั้งหมด
- **🧹 ซ่อนชีทชุดการ์ด**: ซ่อนชีทชุดการ์ดเพื่อความเรียบร้อย
- **🧹 ลบชีทว่าง**: ลบชีทที่ว่างออก

---

## 📁 โครงสร้างชีท

### **ชีทหลัก (System Sheets)**
- **Master**: ข้อมูลการ์ดทั้งหมด (ชุด, รหัสชุด, เลขการ์ด, ชื่อ, จำนวน, ราคา, ฯลฯ)
- **Sets**: รายการชุดการ์ด (ชื่อชุด, รหัสชุด, จำนวนการ์ด)
- **Log**: ประวัติการเปลี่ยนแปลงทั้งหมด
- **RemoveLog**: ประวัติการ์ดที่เอาออก
- **DailyInput**: ข้อมูลรายวันจากฟอร์ม (ก่อน Batch Import)

### **ชีทชุดการ์ด (Set Sheets)**
- ชีทชุดการ์ดต่างๆ เช่น "อัคคีสีคราม (MA2 T)", "วิวัฒนาการเมก้า (MA1 T)"
- ใช้สำหรับนำเข้าข้อมูลเบื้องต้น
- ควรซ่อนหลังนำเข้าข้อมูลแล้ว

---

## 🔑 ฟีเจอร์เด่น

### ✅ **Auto-fill & Suggestions**
- ระบบ Auto-fill ข้อมูลการ์ดเมื่อกรอกรหัสการ์ด
- มี Suggestions สำหรับชื่อการ์ดและชุดการ์ด

### ✅ **Validation & Error Checking**
- ตรวจสอบข้อมูลก่อน Batch Import
- ป้องกันการ์ดซ้ำ
- ตรวจสอบจำนวนสต็อกก่อนส่งออก

### ✅ **Logging**
- บันทึกประวัติทุกการเปลี่ยนแปลง
- แยก Log ตามประเภท (NEW_CARD, UPDATE_EXISTING, REMOVE_CARD, BATCH_IMPORT, ฯลฯ)

### ✅ **Cache System**
- ใช้ Cache เพื่อเพิ่มประสิทธิภาพ
- Auto-refresh เมื่อข้อมูลเปลี่ยนแปลง

---

## 📝 วิธีใช้งาน Workflow หลัก

### **ตัวอย่าง: รับการ์ดใหม่ 10 ใบ**

1. **เช้า**: พิมพ์ฟอร์มรับการ์ดเข้า
   - เมนู → 🖨️ พิมพ์ฟอร์มรับการ์ดเข้า
   - แก้ไข/พิมพ์ → เก็บไว้

2. **เวลารับการ์ด**: พนักงานกรอกฟอร์ม hard copy
   - กรอก: ชุดการ์ด, รหัสชุด, เลขการ์ด, ชื่อการ์ด, จำนวน, ราคา

3. **จบวัน**: กรอกข้อมูลใน DailyInput
   - เมนู → 📋 เปิดชีท DailyInput
   - กรอกข้อมูลจากฟอร์ม
   - ประเภท: "เข้า"
   - สถานะ: "รอตรวจ" (อัตโนมัติ)

4. **Batch Import**: นำเข้าสต็อก
   - เมนู → 📥 นำเข้าข้อมูลรายวัน
   - ตรวจสอบ → นำเข้า
   - ระบบอัพเดท Master อัตโนมัติ

---

## ⚠️ หมายเหตุสำคัญ

1. **DailyInput**: ข้อมูลที่สถานะ "รอตรวจ" เท่านั้นที่จะถูกนำเข้า
2. **การ์ดซ้ำ**: ระบบจะเพิ่มจำนวนให้อัตโนมัติ (สำหรับการ์ดเข้า)
3. **การ์ดออก**: ต้องมีสต็อกเพียงพอ ไม่เช่นนั้นจะแสดงข้อผิดพลาด
4. **Sets Sheet**: ควรอัพเดทเป็นระยะด้วย "🔄 สร้าง/อัปเดต Sets จาก Master"

---

## 🚀 Tips & Best Practices

1. **ใช้ Batch Import**: สำหรับข้อมูลหลายรายการ ใช้ Batch Import แทนการเพิ่มทีละใบ
2. **ตรวจสอบก่อน Import**: ตรวจสอบข้อมูลก่อน Batch Import เสมอ
3. **เก็บ Hard Copy**: เก็บฟอร์ม hard copy ไว้เป็นเอกสารอ้างอิง
4. **อัพเดท Sets**: อัพเดท Sets sheet หลังเพิ่มชุดการ์ดใหม่
5. **ซ่อนชีทชุดการ์ด**: ซ่อนชีทชุดการ์ดหลังนำเข้าข้อมูลแล้ว เพื่อความเรียบร้อย

