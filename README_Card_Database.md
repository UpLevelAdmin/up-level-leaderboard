# 📚 คู่มือการสร้างฐานข้อมูลการ์ดโปเกมอน

## ⚠️ คำเตือนเรื่องลิขสิทธิ์

**การใช้งานนี้อาจมีข้อจำกัดด้านลิขสิทธิ์และ Terms of Service ของเว็บไซต์**
- กรุณาใช้เพื่อการศึกษาและทดสอบเท่านั้น
- ควรได้รับอนุญาตจากเจ้าของสิทธิ์ก่อนใช้งานเชิงพาณิชย์
- การดาวน์โหลดและจัดเก็บภาพการ์ดอาจละเมิดลิขสิทธิ์

---

## 🎯 ภาพรวม

สคริปต์นี้จะ:
1. ดึงข้อมูลการ์ดทั้งหมดจาก **Pokemon TCG API** (api.pokemontcg.io)
2. ดาวน์โหลดรูปภาพการ์ดและเก็บไว้ใน **Google Drive**
3. สร้างฐานข้อมูลใน **Google Sheets** พร้อมข้อมูลการ์ดทั้งหมด
4. สร้างหน้าเว็บสำหรับค้นหาและดูการ์ดจากฐานข้อมูล

---

## 📋 ขั้นตอนการใช้งาน

### 1. สร้าง Google Apps Script

1. ไปที่ [Google Apps Script](https://script.google.com/)
2. คลิก **"New Project"**
3. ตั้งชื่อโปรเจค: `Card Database Scraper`
4. คัดลอกโค้ดจากไฟล์ `app script all/app script Card Database Scraper`
5. วางโค้ดใน Apps Script

### 2. ตั้งค่าการใช้งาน

แก้ไขค่าต่างๆ ในส่วน `CONFIG` (ถ้าต้องการ):

```javascript
var CONFIG = {
  DRIVE_FOLDER_NAME: 'Pokemon Card Database',  // ชื่อโฟลเดอร์ใน Drive
  SPREADSHEET_NAME: 'Pokemon Card Database',   // ชื่อ Spreadsheet
  CARDS_PER_PAGE: 50,                          // จำนวนการ์ดต่อหน้า
  REQUEST_DELAY: 1000,                         // Delay ระหว่าง request (ms)
  MAX_RETRIES: 3                               // จำนวนครั้งที่ลองใหม่
};
```

### 3. ทดสอบการทำงาน

1. เลือกฟังก์ชัน `testScraping` จากเมนู dropdown
2. คลิก **Run** (▶️)
3. อนุญาตการเข้าถึง (Authorize)
   - เลือกบัญชี Google ของคุณ
   - คลิก **Advanced** → **Go to [Project Name] (unsafe)**
   - คลิก **Allow**
4. ตรวจสอบ Logs (View → Logs) เพื่อดูผลลัพธ์

### 4. เริ่มดึงข้อมูลทั้งหมด

⚠️ **คำเตือน:** การดึงข้อมูลทั้งหมดอาจใช้เวลานานมาก (หลายชั่วโมง) เพราะมี:
- การ์ดหลายหมื่นใบ
- Delay ระหว่างการดาวน์โหลดเพื่อหลีกเลี่ยง rate limiting
- การดาวน์โหลดรูปภาพขนาดใหญ่

**ขั้นตอน:**
1. เลือกฟังก์ชัน `startScrapingAllCards` จากเมนู dropdown
2. คลิก **Run** (▶️)
3. รอให้สคริปต์ทำงาน (อาจใช้เวลาหลายชั่วโมง)
4. ตรวจสอบ Logs เพื่อดูความคืบหน้า

**หมายเหตุ:** 
- สามารถหยุดการทำงานได้โดยคลิก **Stop** (⏹️)
- สามารถรันใหม่ได้ (จะข้ามการ์ดที่มีอยู่แล้ว)
- ตรวจสอบ Spreadsheet และ Drive Folder เพื่อดูผลลัพธ์

### 5. Deploy เป็น Web App (สำหรับใช้ในหน้าเว็บ)

1. คลิก **Deploy** → **New deployment**
2. เลือก **Web app**
3. ตั้งค่า:
   - **Execute as:** Me
   - **Who has access:** Anyone
4. คลิก **Deploy**
5. คัดลอก **Web App URL** มาใช้ในหน้าเว็บ

---

## 📊 โครงสร้างฐานข้อมูล

### Google Sheets

Spreadsheet จะมี Sheet ชื่อ `Cards` พร้อมคอลัมน์:

| คอลัมน์ | คำอธิบาย |
|---------|----------|
| Card ID | ID การ์ดจาก API |
| Card Name | ชื่อการ์ด |
| Set Name | ชื่อชุดการ์ด |
| Set Code | รหัสชุดการ์ด |
| Card Number | เลขการ์ดในชุด |
| Rarity | ความหายาก |
| Types | ประเภท (Pokemon, Trainer, Energy) |
| Supertype | ประเภทหลัก |
| Subtypes | ประเภทย่อย |
| HP | HP (สำหรับ Pokemon) |
| Retreat Cost | ค่า Retreat |
| Image URL (Drive) | URL รูปภาพใน Drive |
| Image File Name | ชื่อไฟล์รูปภาพ |
| Original Image URL | URL รูปภาพต้นฉบับ |

### Google Drive

โฟลเดอร์ `Pokemon Card Database` จะมี:
- รูปภาพการ์ดทั้งหมด (ไฟล์ .jpg)
- ชื่อไฟล์: `CardName_SetCode_Number.jpg`
- ทุกไฟล์ตั้งค่าเป็น **"Anyone with the link"** แล้ว

---

## 🌐 การใช้งานในหน้าเว็บ

### ตั้งค่า

1. Deploy Apps Script เป็น Web App (ตามขั้นตอนที่ 5)
2. เปิดไฟล์ `card-database.html`
3. แก้ไข `APPS_SCRIPT_URL` ให้เป็น Web App URL ที่ได้

### ฟีเจอร์

- 🔍 **ค้นหาการ์ด** ตามชื่อหรือชุดการ์ด
- 📄 **Pagination** สำหรับดูการ์ดทีละหน้า
- 🖼️ **แสดงรูปภาพ** จากฐานข้อมูลของเราเอง
- 📱 **Responsive Design** รองรับมือถือ

---

## 🔧 แก้ไขปัญหา

### ปัญหา: Apps Script ไม่สามารถเข้าถึง Drive ได้

**วิธีแก้:**
1. ตรวจสอบว่าได้อนุญาตการเข้าถึงแล้ว
2. ลองรันใหม่และอนุญาตอีกครั้ง
3. ตรวจสอบว่า Apps Script มีสิทธิ์เข้าถึง Drive และ Sheets

### ปัญหา: ดาวน์โหลดรูปภาพล้มเหลว

**วิธีแก้:**
1. ตรวจสอบ Logs เพื่อดู error message
2. อาจเป็นเพราะ rate limiting - เพิ่ม `REQUEST_DELAY`
3. ตรวจสอบว่า URL รูปภาพถูกต้อง

### ปัญหา: การดึงข้อมูลช้า

**วิธีแก้:**
1. ลด `REQUEST_DELAY` (แต่อาจเสี่ยง rate limiting)
2. เพิ่ม `CARDS_PER_PAGE` (ถ้า API รองรับ)
3. รันในเวลาที่ไม่ค่อยมีคนใช้ (เช่น ตอนกลางคืน)

### ปัญหา: ใช้พื้นที่ Drive เต็ม

**วิธีแก้:**
1. ลบรูปภาพเก่าที่ไม่ต้องการ
2. ใช้ Google Drive Storage เพิ่มเติม
3. ลบการ์ดที่ซ้ำกัน

---

## 💡 Tips

1. **สำรองข้อมูล:** สำรอง Spreadsheet และ Drive Folder เป็นประจำ
2. **ทดสอบก่อน:** ใช้ `testScraping()` เพื่อทดสอบก่อนรันทั้งหมด
3. **ตรวจสอบ Logs:** ดู Logs เป็นประจำเพื่อตรวจสอบความคืบหน้า
4. **รันในเวลาที่เหมาะสม:** การดึงข้อมูลทั้งหมดใช้เวลานาน ควรรันในเวลาที่ไม่ต้องใช้คอมพิวเตอร์
5. **อัพเดทฐานข้อมูล:** รันสคริปต์เป็นระยะเพื่ออัพเดทการ์ดใหม่

---

## 📞 API Endpoints

### Search Cards
```
GET [WEB_APP_URL]?action=search&cardName=Pikachu&cardSet=&page=1
```

### Get All Cards
```
GET [WEB_APP_URL]?action=getAll&page=1&pageSize=50
```

---

## 🔄 อัพเดทฐานข้อมูล

เมื่อมีการ์ดใหม่:

1. รัน `startScrapingAllCards()` อีกครั้ง
2. สคริปต์จะเพิ่มการ์ดใหม่เข้าไป (ไม่ซ้ำกับที่มีอยู่แล้ว)
3. หรือแก้ไขโค้ดให้ตรวจสอบการ์ดที่มีอยู่แล้วก่อนเพิ่ม

---

## 📝 หมายเหตุ

- **Pokemon TCG API** มี rate limiting (ประมาณ 100 requests/นาที)
- การดาวน์โหลดรูปภาพใช้เวลาและ bandwidth มาก
- ฐานข้อมูลอาจมีขนาดใหญ่ (หลาย GB)
- ควรสำรองข้อมูลเป็นประจำ

---

## 🚀 ขั้นตอนถัดไป

1. ✅ ดึงข้อมูลการ์ดทั้งหมด
2. ✅ สร้างฐานข้อมูลใน Google Sheets
3. ✅ ดาวน์โหลดรูปภาพไปยัง Google Drive
4. ✅ สร้างหน้าเว็บสำหรับค้นหาและดูการ์ด
5. 🔄 อัพเดทฐานข้อมูลเป็นประจำ

