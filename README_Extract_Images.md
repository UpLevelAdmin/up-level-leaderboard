# 📥 คู่มือดึงรูปภาพจาก Google Sheets

## 🎯 วิธีที่ 1: ดึงด้วยมือ (ง่ายที่สุด)

### ขั้นตอน:

1. **เปิด Google Sheets** ที่มีรูปภาพ
2. **คลิกขวาที่รูปภาพ** ใน cell
3. เลือก **"Save image as..."** หรือ **"บันทึกรูปภาพเป็น..."**
4. บันทึกไฟล์ลงคอมพิวเตอร์
5. อัพโหลดไปที่ Google Drive ตามคู่มือ `README_Image_Setup.md`

**ข้อดี:** ง่าย รวดเร็ว  
**ข้อเสีย:** ถ้ามีรูปเยอะจะใช้เวลานาน

---

## 🚀 วิธีที่ 2: ใช้ Apps Script (แนะนำสำหรับรูปเยอะ)

### ขั้นตอน:

#### 1. เปิด Apps Script

1. เปิด Google Sheets ที่มีรูปภาพ
2. ไปที่ **Extensions** → **Apps Script**
3. สร้างไฟล์ใหม่ชื่อ `Extract Images`

#### 2. วางโค้ด

1. คัดลอกโค้ดจากไฟล์ `app script all/app script Extract Images`
2. วางใน Apps Script
3. **แก้ไขค่าเหล่านี้:**
   ```javascript
   var spreadsheetId = '1JvVCVxxXBS-2lpnT-IOrYBf2rF-OBRVFCW6MZEvpqxE'; // ID ของ Sheet
   var sheetName = 'ฝากขายการ์ด'; // ชื่อ Sheet
   var imageColumn = 6; // Column F = 6
   ```

#### 3. รันสคริปต์

1. คลิก **Run** (▶️) หรือกด `Ctrl+R` / `Cmd+R`
2. อนุญาตการเข้าถึง (Authorize)
3. เลือกบัญชี Google ของคุณ
4. คลิก **Advanced** → **Go to [Project Name] (unsafe)**
5. คลิก **Allow**

#### 4. ตรวจสอบผลลัพธ์

1. ไปที่ Google Drive
2. หาโฟลเดอร์ **"Extracted Images"**
3. รูปภาพทั้งหมดจะอยู่ในโฟลเดอร์นี้
4. แต่ละไฟล์จะตั้งค่าเป็น **"Anyone with the link"** แล้ว

#### 5. อัพเดท URL ใน Google Sheet (Optional)

ถ้าต้องการให้อัพเดท URL อัตโนมัติ:

1. รันฟังก์ชัน `updateImageUrlsInSheet()`
2. ระบบจะอัพเดทคอลัมน์ F ให้เป็น `=IMAGE("URL")` อัตโนมัติ

---

## 📋 วิธีที่ 3: ดึงทีละหลายรูป (Manual Batch)

### ขั้นตอน:

1. **เลือกหลาย cell** ที่มีรูปภาพ (กด Ctrl/Cmd ค้างไว้)
2. **คลิกขวา** → **"Download"** หรือ **"ดาวน์โหลด"**
3. Google Sheets จะดาวน์โหลดเป็นไฟล์ ZIP
4. แตกไฟล์ ZIP
5. อัพโหลดรูปภาพทั้งหมดไปที่ Google Drive

---

## 🔧 แก้ไขปัญหา

### ปัญหา: Apps Script ไม่สามารถเข้าถึง Drive ได้

**วิธีแก้:**
1. ตรวจสอบว่าได้อนุญาตการเข้าถึงแล้ว
2. ลองรันใหม่และอนุญาตอีกครั้ง

### ปัญหา: รูปภาพไม่แสดงใน Drive

**วิธีแก้:**
1. ตรวจสอบว่าไฟล์ตั้งค่าเป็น "Anyone with the link" แล้วหรือยัง
2. ลองเปิด URL ใน Incognito mode

### ปัญหา: ชื่อไฟล์ไม่ถูกต้อง

**วิธีแก้:**
1. เปลี่ยนชื่อไฟล์ใน Drive ให้ตรงกับชื่อการ์ด
2. หรือแก้ไขโค้ด Apps Script ในส่วน `fileName`

---

## 💡 Tips

1. **สำรองข้อมูลก่อน:** สำรอง Google Sheet ก่อนรันสคริปต์
2. **ทดสอบก่อน:** ลองรันกับ 1-2 รูปก่อน ถ้าทำงานได้ค่อยรันทั้งหมด
3. **ตั้งชื่อไฟล์ให้เป็นระเบียบ:** เช่น `Pikachu_UR_001.png`
4. **ใช้โฟลเดอร์แยก:** สร้างโฟลเดอร์แยกตามประเภทการ์ด

---

## 📞 ถ้ายังมีปัญหา

1. ตรวจสอบว่า Apps Script มีสิทธิ์เข้าถึง Drive และ Sheets
2. ตรวจสอบ console log ใน Apps Script (View → Logs)
3. ลองใช้วิธีที่ 1 (ดึงด้วยมือ) ก่อนเพื่อทดสอบ

