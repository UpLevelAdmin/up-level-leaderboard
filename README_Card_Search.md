# 🔍 ระบบค้นหาการ์ดโปเกมอน

ระบบค้นหาการ์ดโปเกมอนที่ดึงข้อมูลจาก Pokemon TCG API (ถูกต้องตามกฎหมาย)

## ⚠️ คำเตือนเรื่องลิขสิทธิ์

- ระบบนี้ใช้ **Pokemon TCG API** ซึ่งเป็น API ที่เปิดให้ใช้ฟรีและถูกต้องตามกฎหมาย
- **ไม่แนะนำ** ให้ดึงข้อมูลจากเว็บ Pokemon Card Asia โดยตรง เพราะอาจละเมิดลิขสิทธิ์และ Terms of Service
- ใช้เพื่อการศึกษาและทดสอบเท่านั้น

## 📋 ไฟล์ที่เกี่ยวข้อง

1. **card-search.html** - หน้าเว็บสำหรับค้นหาการ์ด
2. **app script Card Search** - Google Apps Script สำหรับดึงข้อมูลจาก API

## 🚀 วิธีการติดตั้งและใช้งาน

### ขั้นตอนที่ 1: Deploy Google Apps Script

1. เปิด Google Apps Script: https://script.google.com
2. สร้างโปรเจกต์ใหม่
3. Copy โค้ดจากไฟล์ `app script Card Search` มาใส่
4. คลิก **Deploy** > **New deployment**
5. เลือกประเภท **Web app**
6. ตั้งค่า:
   - **Execute as**: Me
   - **Who has access**: Anyone
7. คลิก **Deploy**
8. Copy **Web App URL** ที่ได้

### ขั้นตอนที่ 2: ตั้งค่า URL ในหน้าเว็บ

1. เปิดไฟล์ `card-search.html`
2. หาบรรทัดที่มี:
   ```javascript
   const APPS_SCRIPT_URL = 'YOUR_APPS_SCRIPT_WEB_APP_URL';
   ```
3. แทนที่ `YOUR_APPS_SCRIPT_WEB_APP_URL` ด้วย URL ที่ copy มา
4. บันทึกไฟล์

### ขั้นตอนที่ 3: ทดสอบใช้งาน

1. เปิดไฟล์ `card-search.html` ในเบราว์เซอร์
2. ใส่ชื่อการ์ด (เช่น "Pikachu") หรือชุดการ์ด (เช่น "Base Set")
3. คลิกปุ่ม **🔍 ค้นหา**
4. ระบบจะแสดงผลการ์ดที่ค้นหาได้

## 🎯 ฟีเจอร์

- ✅ ค้นหาการ์ดตามชื่อ
- ✅ ค้นหาการ์ดตามชุดการ์ด
- ✅ แสดงภาพการ์ดแบบ grid
- ✅ คลิกเพื่อดูภาพการ์ดขนาดใหญ่
- ✅ Responsive design (รองรับมือถือ)

## 🔧 การแก้ไขปัญหา

### ปัญหา: CORS Error

**สาเหตุ:** เรียก API โดยตรงจากเบราว์เซอร์

**วิธีแก้:**
- ใช้ Google Apps Script เป็น proxy (แนะนำ)
- หรือใช้ CORS proxy อื่นๆ

### ปัญหา: ไม่พบการ์ด

**สาเหตุ:** ชื่อการ์ดหรือชุดการ์ดไม่ถูกต้อง

**วิธีแก้:**
- ลองใช้ชื่อภาษาอังกฤษ
- ลองใช้ชื่อย่อของชุดการ์ด
- ตรวจสอบว่ามีการ์ดในชุดนั้นจริงๆ

### ปัญหา: Apps Script ไม่ทำงาน

**สาเหตุ:** ยังไม่ได้ deploy หรือ URL ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบว่า Apps Script ถูก deploy แล้ว
2. ตรวจสอบว่า URL ใน card-search.html ถูกต้อง
3. ตรวจสอบว่า Apps Script มี permission ที่ถูกต้อง

## 📝 หมายเหตุ

- ระบบใช้ **Pokemon TCG API** ซึ่งเป็น API ฟรีและถูกต้องตามกฎหมาย
- API นี้มีข้อมูลการ์ดและภาพการ์ดจาก Pokemon Company
- ไม่จำเป็นต้องใช้ API key สำหรับการใช้งานพื้นฐาน
- ถ้าต้องการใช้งานมากๆ อาจต้องใช้ API key (ดูที่ https://pokemontcg.io)

## 🔗 ลิงก์ที่เกี่ยวข้อง

- Pokemon TCG API: https://pokemontcg.io
- Google Apps Script: https://script.google.com
- Pokemon Card Asia: https://asia.pokemon-card.com/th/deck-build/

## 📞 ติดต่อ

หากมีปัญหาหรือคำถาม กรุณาติดต่อแอดมิน

